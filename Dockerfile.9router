# Local build for 9router since Docker Hub pull has auth issues
FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache git curl

RUN npm install -g 9router

ENV PORT=20128
ENV HOSTNAME=0.0.0.0
ENV DATA_DIR=/app/data

RUN mkdir -p /app/data

EXPOSE 20128

CMD ["9router"]
